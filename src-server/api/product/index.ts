import './productCreate'
import './productGetList'
import './productGet' // Must be last because dynamic "/:id" route
import './productUpdate' // Must be last because dynamic "/:id" route
import './productDelete' // Must be last because dynamic "/:id" route

export * from './router'
